import { ApiEnvironment } from '@rosa/api-client';

export const environment: ApiEnvironment = {
  apiUrl: 'http://localhost:3000/api/v1', // now includes version
  // Add other environment variables as needed
};
